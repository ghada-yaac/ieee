{% extends 'base.html.twig' %}

{% block title %}Membre{% endblock %}

{% block body %}
    <h1>Membre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ membre.id }}</td>
            </tr>
            <tr>
                <th>MemberID</th>
                <td>{{ membre.memberID }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ membre.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ membre.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ membre.email }}</td>
            </tr>
            <tr>
                <th>Mdp</th>
                <td>{{ membre.mdp }}</td>
            </tr>
            <tr>
                <th>NumTel</th>
                <td>{{ membre.numTel }}</td>
            </tr>
            <tr>
                <th>MembershipStatus</th>
                <td>{{ membre.membershipStatus }}</td>
            </tr>
            <tr>
                <th>MembershipStartDate</th>
                <td>{{ membre.membershipStartDate ? membre.membershipStartDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>MembershipEndDate</th>
                <td>{{ membre.membershipEndDate ? membre.membershipEndDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ membre.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_membre_index') }}">back to list</a>

    <a href="{{ path('app_membre_edit', {'id': membre.id}) }}">edit</a>

    {{ include('membre/_delete_form.html.twig') }}
{% endblock %}
